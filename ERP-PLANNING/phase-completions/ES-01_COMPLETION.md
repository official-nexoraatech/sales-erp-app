# ES-01 Completion Report — Critical Security & Routing Fixes
**Date:** 2026-07-02
**Phase:** ES-01 of 20
**Status:** COMPLETE

## Summary
Closed an unauthenticated internal-service gap (search-service accepted requests with no JWT check on any of its 7 routes), tightened the login rate limit from 100/window to 10 per 15 minutes, fixed a React Router route-ordering bug that made `/reports/schedules` unreachable (shadowed by `/reports/:slug`), added a dismissible STUB warning banner to the e-Invoice page pending real NIC integration (ES-11), and added a data-staleness indicator to the dashboard.

## Files Changed
| File | Change |
|------|--------|
| `apps/search-service/src/middleware/authenticate.ts` | NEW — RS256 JWT middleware (jose library) |
| `apps/search-service/src/api/search.routes.ts` | Added `preHandler: [authenticate]` to all 7 routes |
| `apps/search-service/package.json` | Added `jose ^5.9.6` dependency |
| `.env` | `LOGIN_RATE_LIMIT_MAX` 100 → 10; window set to 900000ms (15 min) |
| `.env.example` | Added `LOGIN_RATE_LIMIT_MAX=10` and `LOGIN_RATE_LIMIT_WINDOW_MS=900000` with comments |
| `apps/web-frontend/src/App.tsx` | Moved `/reports/schedules` route before `/reports/:slug` |
| `apps/web-frontend/src/pages/gst/EInvoicePage.tsx` | Added dismissible amber STUB warning banner (sessionStorage-based) |
| `apps/web-frontend/src/pages/DashboardPage.tsx` | Added staleness badge shown when `dataUpdatedAt` > 30s ago |
| `apps/search-service/src/__tests__/search-auth.test.ts` | NEW — 3 integration tests (no auth → 401, bad prefix → 401, valid Bearer → 200) |

## Tests Added
- [x] search-auth.test.ts: 3 tests (no Authorization header → 401; malformed/bad-prefix header → 401; valid Bearer token → 200)

## Verification Results
- [x] All 7 routes in `search.routes.ts` have `preHandler: [authenticate]`
- [x] `jose ^5.9.6` present in `apps/search-service/package.json`
- [x] `.env` and `.env.example` both have `LOGIN_RATE_LIMIT_MAX=10` / `LOGIN_RATE_LIMIT_WINDOW_MS=900000`
- [x] `App.tsx` — `reports/schedules` route registered before `reports/:slug`
- [x] `EInvoicePage.tsx` — STUB banner present, dismissal persisted via `sessionStorage`
- [x] `DashboardPage.tsx` — staleness check (`STALE_THRESHOLD_MS = 30_000`) and "Data may be stale" indicator present

## Key Facts for Subsequent Phases
- Search service now **requires** a valid Bearer JWT on every route — internal callers must pass tokens
- Login rate limit is `10 requests per 15 minutes`
- `/reports/schedules` is navigable; `/reports/:slug` still works for all other slugs
- e-Invoice page has a STUB banner — **ES-11 is responsible for removing it** when NIC integration is live
- Dashboard has a staleness indicator polling every 5s when data age > 30s
- The ERP-wide ESLint config lacks `globals: { process: 'readonly' }` (pre-existing issue) — add `/* global process */` when encountered in new files

## Issues Encountered
None — this report was generated retroactively on 2026-07-03 after verifying all claimed changes exist in the working tree. The phase's code was implemented on 2026-07-02 but no completion report had been saved, even though ES-02 through ES-19 all reference `ES-01_COMPLETION.md` as required reading.

## Phases Now Unblocked
ES-02, ES-04, ES-06, ES-07 (all independent phases already assumed ES-01 was done)

## Notes for Next Phase (ES-03)
- No direct dependency on ES-01 beyond the shared security baseline.
- ES-11 (NIC e-Invoice/e-Way Bill) must remove the STUB banner in `EInvoicePage.tsx` when it ships real IRN generation.

## Final Summary
Search-service is no longer an unauthenticated gap in the tenant boundary, brute-force login risk is reduced 10x, and the reports routing bug that hid the Schedules page is fixed. This phase is a pure security/bugfix baseline with no schema or API contract changes — nothing for downstream phases to integrate against beyond "auth is now required on search-service."

---
*Generated by: Claude Sonnet 5 | Date: 2026-07-03 | Retroactive report — implementation completed 2026-07-02*
