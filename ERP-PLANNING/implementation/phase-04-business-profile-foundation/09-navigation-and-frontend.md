# 09 — Navigation and Frontend

## No change

This phase is backend/schema only. `web-frontend`'s tenant-provisioning form (if one exists — `TO VERIFY`, not located this session; provisioning may be admin-CLI/API-only today) is unaffected, since the `vertical` field it already submits continues to work unchanged (`05-service-impact.md`). No nav entry, no new page, no `capabilityKey`/`moduleCode` usage — this phase predates and is independent of any UI surface for business types (a future phase, if a platform-operator UI for choosing/managing business types is ever built, would be the first to touch `web-frontend` for this concept).
