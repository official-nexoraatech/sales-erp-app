# 13 — Search Impact

## Out of scope, confirmed

`search-service` indexes items/customers/other tenant-scoped searchable entities — `industries`/`business_types` are global reference data, not tenant-scoped searchable content, and this phase adds no new tenant-facing entity for it to index. No change needed or made.
