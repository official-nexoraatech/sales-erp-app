# 31 — Revised Acceptance Criteria

Supersedes `20-acceptance-criteria.md`. Same A–I mapping to the governing brief's SECOND section, now split by phase (`25-revised-scope.md` §6) and with the gate review's five-missing-flow gap closed. **This document does not itself grant implementation authorization** — see §0.

## 0. Preconditions before any phase begins

| Precondition                                                                                | Status                                                                                              |
| ------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| D1 (sales-service reconciliation approach) confirmed by user                                | **Open** — recommendation given (`26-decision-record.md`), not yet user-confirmed                   |
| D4 (capability-disable behavior) confirmed by user                                          | **Open** — recommendation given, not yet user-confirmed (non-blocking for 2A, blocking for 2B ship) |
| D2 (expiry policy) resolved or explicitly ratified as "not in v1, ordering-preference only" | **Open** — not blocking 2A/2B provided the disclosure requirement (`30` Step 2B.6) ships with 2B    |

**Neither Phase 2A nor Phase 2B may be marked "READY FOR IMPLEMENTATION" until D1 is confirmed.** This document intentionally does not claim readiness — it defines what "done" looks like once the user unblocks it.

---

## Phase 2A acceptance criteria

| #    | Must prove                                                                                                                    | Verifiable check                                                                                                                                                                   |
| ---- | ----------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2A-1 | The two valuation engines' behavioral differences are fully catalogued, not just the ones already known                       | Written diff document/commit message enumerating every difference found in Step 2A.1, with each one either reconciled or explicitly deferred with reasoning                        |
| 2A-2 | Sales-service consumption is byte-identical to pre-migration behavior for the only state possible today (`fefoEnabled=false`) | `valuation-migration-equivalence.test.ts` passes; zero assertion changes needed in any pre-existing sales-service test                                                             |
| 2A-3 | FEFO ordering works through the real `InvoiceService`/`SaleReturnService`/POS call path, not just the underlying engine       | `valuation-fefo.test.ts` (sales-service) passes, exercising the same scenario `valuation-engine-fefo.test.ts` already proves at the engine level                                   |
| 2A-4 | Sales-return restock preserves batch/expiry traceability                                                                      | `sale-return-batch-traceability.test.ts` passes                                                                                                                                    |
| 2A-5 | Zero behavior change for any existing tenant on merge                                                                         | Confirmed by construction — no item can be `fefoEnabled: true` before Phase 2B ships; full regression suite (`git stash -u` discipline) shows no unexpected diffs                  |
| 2A-6 | The local duplicate is fully removed (if D1(B) chosen) or fully reconciled (if D1(A) chosen)                                  | `grep -r "from './ValuationService.js'"` returns zero matches (D1(B)) or the local file's `consumeFifoLayers`/`StockInValuationParams` match the shared engine's field set (D1(A)) |

## Phase 2B acceptance criteria (mapped to the brief's A–I, revised)

| #            | Must prove                                                                                                    | Verifiable check                                                                                                                                                                                                                                                    |
| ------------ | ------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A            | A capability can be defined                                                                                   | `CAPABILITY_REGISTRY['INVENTORY_BATCH']` exists, registry-completeness test extended to 3 entries                                                                                                                                                                   |
| B            | A tenant can have that capability enabled                                                                     | Global flag seeded; tenant-specific override provable by integration test                                                                                                                                                                                           |
| C            | A user can have the required permission                                                                       | `BATCH_VIEW`/`BATCH_CONFIGURE` in `permissions.ts`, granted in `ROLE_DEFAULTS` + existing-tenant migration backfill                                                                                                                                                 |
| D            | Backend service enforcement works                                                                             | `GET /inventory/near-expiry-stock` full A–E outcome matrix; `POST/PUT /items` rejects `fefoEnabled: true` with capability off                                                                                                                                       |
| E            | Frontend navigation responds to capability state                                                              | Nav item hidden/shown per `enabledCapabilities`; manual in-browser verification                                                                                                                                                                                     |
| F            | Existing tenants remain unaffected                                                                            | Full regression across `inventory-service`/`purchase-service`/`production-service`/`sales-service`/`tenant-service`/`web-frontend` — **now including the nine-flow FEFO test matrix**, not just the three services the original plan tracked                        |
| **F2 (new)** | **All nine affected flows have explicit FEFO test coverage, not just item-route and near-expiry-route tests** | Every row in `27-affected-flow-matrix.md`'s cross-cutting summary table has a passing FEFO-ordering test and a `fefoEnabled=false` regression test — closes the single largest gap the gate review found                                                            |
| G            | The capability can be used by a genuinely different business model                                            | Documented reuse path unchanged (`02-business-requirements.md` §3)                                                                                                                                                                                                  |
| H            | The implementation does not require an industry fork                                                          | Confirmed by construction, unchanged                                                                                                                                                                                                                                |
| I            | The same capability infrastructure can support future industries                                              | Registry grows 2→3 entries (unchanged mechanism claim, now 3rd entry not 2nd since HR_PAYROLL/POS remain the only two registered today — confirmed by direct registry read this session)                                                                            |
| **J (new)**  | **The financial-neutrality claim is corrected, not asserted false-but-unfixed**                               | `07-api-contracts.md` §4's "financially neutral" language is corrected per `28-financial-impact-analysis.md` before this phase is called done — either by editing that document or by this document set superseding it explicitly (both acceptable; silence is not) |
| **K (new)**  | **The expired-stock scope boundary is disclosed, not silently assumed**                                       | Item-form UI copy and/or capability documentation states plainly that FEFO reorders consumption preference and does not block expired stock — per `29-expiry-policy-analysis.md` §4 item 4                                                                          |

## Explicitly deferred, not part of either phase's acceptance criteria

- Phase 2C (any expiry-blocking policy) — blocked on D2, not scoped here.
- D3 (batch-targeted correction workflows for adjustments/physical-verification/purchase-returns) — recorded, not required.
- Job-work finished-goods batch/expiry threading (`27-affected-flow-matrix.md` #7) — a distinct, newly-identified gap, not required for Phase 2A/2B, should be tracked as its own follow-up item.
- Stock-transfer batch/expiry carry-forward to the destination warehouse's new layer (`27-affected-flow-matrix.md` #1) — same treatment as the above.
