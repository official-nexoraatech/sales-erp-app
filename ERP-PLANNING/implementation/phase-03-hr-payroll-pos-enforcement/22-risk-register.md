# 22 — Risk Register

| Risk                                                                                                                                                                        | Probability                                                                                                                                                   | Impact                                                                                                                                                                                       | Detection                                                                                              | Mitigation                                                                                                                                                   | Owner                                      | Rollback                                                                                                             |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------- |
| A real, currently-active tenant's `pos.enabled`/`hr.payroll.enabled` is `false` and is broken by enforcement (D1's core risk)                                               | Medium (unproven in production, but the mechanism that would have caught it — nothing — has never existed)                                                    | **HIGH** (revenue-critical checkout or payroll failure)                                                                                                                                      | `erp_capability_check_denied_total` spike; support ticket correlated with `403 CAPABILITY_NOT_ENABLED` | D1's plan-derived backfill + optional shadow-mode observation window (`19-rollout-and-rollback.md`)                                                          | Implementer, confirmed by user's D1 answer | Code rollback (remove preHandler) is immediate; data was never wrong in this scenario, only enforcement timing       |
| A `STARTER`/`GROWTH` tenant is genuinely using POS/Payroll outside its plan's entitlement today, and enforcement correctly (but disruptively) blocks it                     | Low-Medium (zero evidence of this in the dev dataset, unknown in production)                                                                                  | MEDIUM (a real customer conversation, not an engineering failure)                                                                                                                            | Same metric, tenant-by-tenant                                                                          | Shadow-mode window surfaces this before hard enforcement (D1 option b); customer success/ops process to upsell or grandfather, not an engineering decision   | Product/business owner, not this phase     | N/A — this is enforcement doing its job; "rollback" here means a business decision to grandfather, not a code revert |
| Existing POS/HR test fixtures don't set the relevant flag `true`, causing false-negative test failures mistaken for a real regression                                       | **High** (confirmed by direct grep this session — `16-testing-strategy.md`: zero references in 3 named test files)                                            | LOW-MEDIUM (wastes implementation-time debugging effort, not a production risk)                                                                                                              | CI failure on the flagged files                                                                        | Fix identified proactively in `16-testing-strategy.md`/`21-file-level-change-plan.md` before implementation starts                                           | Implementer                                | N/A                                                                                                                  |
| `assignPlanEntitlements` re-run for an unrelated reason (e.g., a plan change) overwrites D1's backfill if plan templates aren't independently verified to include the flags | Low-Medium (`TO VERIFY` in `10-entitlement-impact.md`, not yet confirmed either way)                                                                          | MEDIUM (silent regression days/weeks after this phase ships, hard to trace back to root cause)                                                                                               | A tenant that previously worked suddenly gets `403` with no code deploy nearby                         | Verify `plan_entitlements` seed data as part of D1's implementation, not left as a dangling `TO VERIFY`                                                      | Implementer                                | Re-apply the backfill; consider whether `plan_entitlements` itself needs a data fix too                              |
| Tenant-1-shaped gap exists for other tenants beyond the one found in dev data (a plan-entitled tenant with no `feature_flags` row at all, for other flags too)              | Medium (only two flags checked this session; the same drift class plausibly affects other `plan_entitlements` keys, out of this phase's scope to fix broadly) | LOW for this phase specifically (the backfill's `INSERT ... NOT EXISTS` branch handles it for these two keys); MEDIUM as a signal of a broader, unaddressed drift class                      | Direct SQL audit across all `plan_entitlements.feature_flags` keys vs. real `feature_flags` rows       | Out of scope to fix broadly here — flag as a finding for a future, dedicated "entitlement drift audit," don't silently expand this phase's scope to cover it | N/A (recommend a separate initiative)      | N/A                                                                                                                  |
| `pos-frontend`'s error handling for the new `403` is missed or crashes the till UI                                                                                          | Low (small, bounded, testable change)                                                                                                                         | MEDIUM (a till operator facing a broken screen mid-shift is a real operational problem even if rare)                                                                                         | Manual smoke test (required by `20-acceptance-criteria.md`)                                            | Explicit acceptance criterion, not left implicit                                                                                                             | Implementer                                | Code rollback of the gate itself removes the trigger entirely                                                        |
| AI Copilot has an unverified tool registered against one of these 18 routes (`14-ai-copilot-impact.md`'s open check)                                                        | Low (Phase 1/2B both found zero such registrations for their own routes)                                                                                      | LOW (the mechanism fails safely even if a tool exists — no bypass, just an unhandled-in-copilot-UX error)                                                                                    | Grep at implementation time                                                                            | Explicit `TO VERIFY` item, not assumed clear                                                                                                                 | Implementer                                | N/A                                                                                                                  |
| Internal payroll scheduler jobs (D2) continue running for a capability-disabled tenant                                                                                      | Medium (deliberately deferred, not fixed)                                                                                                                     | LOW (no `ENTERPRISE`-only feature is being denied to someone unentitled — the internal job only matters for tenants that already have payroll data, i.e., already-entitled ones in practice) | N/A — not instrumented differently by this phase                                                       | Explicitly recorded as D2, revisit only if a real incident demonstrates it's insufficient                                                                    | N/A (deferred)                             | N/A                                                                                                                  |

## Financial/business risk trace (Rule 8)

The only financial-correctness-relevant risk in this register is the first row (availability, not computation) — no risk here resembles Phase 2B's FEFO cost-reordering finding, because this phase changes zero calculation logic anywhere (`26-affected-flow-matrix.md`'s financial trace).
