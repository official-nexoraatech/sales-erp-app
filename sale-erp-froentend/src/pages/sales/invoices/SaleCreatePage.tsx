import React from 'react'; import { useMutation } from '@tanstack/react-query'; import { useNavigate } from 'react-router-dom'; import toast from 'react-hot-toast'; import { salesApi } from '../../../api/endpoints'; import { SaleForm } from './SaleForm';
export const SaleCreatePage: React.FC = () => { const navigate=useNavigate(); const mutation=useMutation({mutationFn:salesApi.create,onSuccess:()=>{toast.success('Sale created successfully');navigate('/sales/invoices')},onError:(e:any)=>toast.error(e?.message||'Failed to create sale')}); return <div className="space-y-5"><div className="text-sm text-gray-500">Home › Sale › Invoices › Create Sale</div><SaleForm submitText="Submit" loading={mutation.isPending} onSubmit={(p)=>mutation.mutate(p)} onCancel={()=>navigate('/sales/invoices')} /></div> };
